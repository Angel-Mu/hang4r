// The icon set lives in src/shared/icons.tsx so the mobile app renders the
// exact same glyphs — this shim keeps every renderer import path working.
export { Icon, type IconName } from '../../../shared/icons'
